# 버전 핀(==) — 재현 가능한 컨테이너 빌드를 위해 정확 핀 유지.
# (과거: 미핀 시 fastapi/starlette 1.x가 @app.on_event 를 제거 → 502. 현재는
#  lifespan 핸들러로 이전(backend/main.py)해 on_event 의존을 없앴으므로 그 사유는 해소됨.
#  핀은 이제 안정성 목적이며, 업그레이드 시 Docker 빌드로 검증 후 일괄 갱신 권장.)
# 검증된 조합(자매앱 arch-law-diagnose와 동일 라인).
fastapi==0.115.5
uvicorn[standard]==0.32.1
anthropic==0.40.0
python-dotenv==1.0.1
voyageai==0.3.2      # 벡터 RAG 질의 임베딩(런타임). 0.4 메이저 점프 방지. 미설정 시 키워드 FTS 폴백
numpy>=1.26,<3       # 임베딩 코사인 top_k
httpx==0.27.2        # VWorld API 비동기 HTTP 클라이언트
